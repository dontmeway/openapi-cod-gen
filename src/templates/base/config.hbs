type Headers = Record<string, any>

export type ConfigType = {
    BASE_URL: string
    LOCALE?: string
    HEADERS?: Headers
}

export const Config: ConfigType = {
    BASE_URL: '',
    LOCALE: undefined,
    HEADERS: undefined,
}
