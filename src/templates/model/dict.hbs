{
  {{#if props}}
    {{#each props}}
      {{{this.name}}}{{#unless this.required}}?{{/unless}}:
      {{#equals this.ref 'array'}}Array<{{{this.type}}}>{{else}}{{{this.type}}}{{/equals}}{{#if this.nullable}} | null{{/if}}
    {{/each}}
  {{/if}}
}
